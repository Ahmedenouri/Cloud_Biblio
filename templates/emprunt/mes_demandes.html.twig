{% extends 'base.html.twig' %}

{% block title %}Mes Activités | BiblioHub{% endblock %}

{% block body %}
<div class="max-w-6xl mx-auto px-6 py-10 min-h-screen">
    <h1 class="text-3xl font-bold text-biblio-dark mb-8 border-b border-gray-200 pb-4">Historique de mes activités</h1>

    <div class="mb-12">
        <h2 class="text-2xl font-bold text-biblio-dark mb-4 flex items-center gap-2">
            <span>📚</span> Mes Emprunts
        </h2>
        
        <div class="bg-white rounded-lg shadow overflow-hidden border border-gray-100">
            <table class="min-w-full">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Date demande</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Livre</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Retour Prévu</th>
                        <th class="px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider">Statut</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-200">
                    {% for emprunt in emprunts %}
                        <tr class="hover:bg-gray-50">
                            <td class="px-6 py-4 text-sm text-gray-500">
                                {{ emprunt.dateEmprunt|date('d/m/Y') }}
                            </td>
                            <td class="px-6 py-4">
                                <div class="text-sm font-bold text-gray-900">{{ emprunt.livre.titre }}</div>
                            </td>
                            <td class="px-6 py-4 text-sm text-gray-600">
                                {{ emprunt.dateRetourPrevue|date('d/m/Y') }}
                            </td>
                            <td class="px-6 py-4 text-center">
                                {% if emprunt.statut == 'EN_ATTENTE' %}
                                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-yellow-100 text-yellow-800">
                                        ⏳ En attente
                                    </span>
                                {% elseif emprunt.statut == 'VALIDE' %}
                                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                        ✅ Validé
                                    </span>
                                {% elseif emprunt.statut == 'REFUSE' %}
                                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-red-100 text-red-800">
                                        ❌ Refusé
                                    </span>
                                {% else %}
                                    <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-gray-100 text-gray-800">
                                        {{ emprunt.statut }}
                                    </span>
                                {% endif %}
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="4" class="px-6 py-8 text-center text-gray-500 italic">
                                Vous n'avez aucun emprunt en cours.
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

    <div>
        <h2 class="text-2xl font-bold text-biblio-dark mb-4 flex items-center gap-2">
            <span>🛒</span> Mes Achats & Téléchargements
        </h2>
        
        <div class="bg-white rounded-lg shadow overflow-hidden border border-gray-100">
            <table class="min-w-full">
                <thead class="bg-gray-50">
                    <tr>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Date Achat</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Détails</th>
                        <th class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">Total</th>
                        <th class="px-6 py-3 text-center text-xs font-medium text-gray-500 uppercase tracking-wider">Statut</th>
                    </tr>
                </thead>
                <tbody class="divide-y divide-gray-200">
                    {% for commande in commandes %}
                        <tr class="hover:bg-gray-50">
                            <td class="px-6 py-4 text-sm text-gray-500">
                                {{ commande.dateCommande|date('d/m/Y H:i') }}
                            </td>
                            <td class="px-6 py-4">
                                <ul class="text-sm text-gray-700 list-none space-y-3">
                                    {% for ligne in commande.ligneCommandes %}
                                        <li class="flex flex-col border-b border-gray-100 pb-2 last:border-0 last:pb-0">
                                            <span class="mb-1">
                                                <span class="font-bold bg-gray-200 px-2 py-0.5 rounded text-xs mr-1">{{ ligne.quantite }}x</span> 
                                                {{ ligne.livre.titre }}
                                            </span>
                                            
                                            {# --- AJOUT DU BOUTON TÉLÉCHARGER ICI --- #}
                                            {% if ligne.livre.pdf %}
                                                <a href="{{ path('app_livre_download', {'id': ligne.livre.id}) }}" target="_blank" class="text-cyan-600 hover:text-cyan-800 font-bold text-xs flex items-center gap-1 ml-8 transition-colors">
                                                    <span>📥</span> Télécharger le PDF
                                                </a>
                                            {% endif %}
                                        </li>
                                    {% endfor %}
                                </ul>
                            </td>
                            <td class="px-6 py-4 text-sm font-bold text-biblio-dark">
                                {{ commande.total }} MAD
                            </td>
                            <td class="px-6 py-4 text-center">
                                <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium bg-green-100 text-green-800">
                                    ✅ Payé & Validé
                                </span>
                            </td>
                        </tr>
                    {% else %}
                        <tr>
                            <td colspan="4" class="px-6 py-8 text-center text-gray-500 italic">
                                Vous n'avez effectué aucun achat.
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
            </table>
        </div>
    </div>

</div>
{% endblock %}