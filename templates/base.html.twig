<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}BiblioHub{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>📚</text></svg>">
        
        {# --- CONFIGURATION TAILWIND CSS --- #}
        <script src="https://cdn.tailwindcss.com"></script>
        <script>
          tailwind.config = {
            theme: {
              extend: {
                colors: {
                  biblio: {
                    dark: '#8B5E3C',      /* Marron foncé */
                    light: '#F5F1E6',     /* Beige clair */
                    accent: '#A67C52',    /* Marron moyen */
                    text: '#F5F1E6',      /* Texte clair */
                  }
                }
              }
            }
          }
        </script>
        
        {% block stylesheets %}{% endblock %}
        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="bg-biblio-light font-sans text-gray-800 flex flex-col min-h-screen">
        
        {# --- NAVBAR GLOBALE --- #}
        <nav class="bg-biblio-dark shadow-md sticky top-0 z-50">
            <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
                <div class="flex items-center justify-between h-16">
                    
                    <div class="flex items-center gap-6">
                        <a href="{{ path('app_home') }}" class="flex items-center gap-2 text-biblio-text text-xl font-bold hover:opacity-90 transition mr-4">
                            <span class="text-2xl">📚</span>
                            <span class="hidden sm:inline">BiblioHub</span>
                        </a>

                        <div class="hidden md:flex items-center gap-2">
                            <a href="{{ path('app_home') }}" class="text-biblio-text hover:bg-biblio-accent px-3 py-2 rounded-md text-sm font-medium transition">
                                Catalogue
                            </a>
                            
                            {% if is_granted('ROLE_BIBLIOTHECAIRE') %}
                                <span class="text-biblio-text/30 mx-1">|</span> <a href="{{ path('app_livre_index') }}" class="text-yellow-100 hover:text-white hover:bg-biblio-accent px-3 py-2 rounded-md text-sm font-bold transition flex items-center gap-1">
                                    <span>📚</span> Gestion Livres
                                </a>
                                <a href="{{ path('app_emprunt_index') }}" class="text-yellow-100 hover:text-white hover:bg-biblio-accent px-3 py-2 rounded-md text-sm font-bold transition flex items-center gap-1">
                                    <span>📋</span> Gestion Emprunts
                                </a>
                            {% endif %}
                        </div>
                    </div>

                    <div class="flex items-center gap-4">
                        
                        {% if app.user %}
                            <a href="{{ path('app_mes_demandes') }}" class="hidden md:flex items-center gap-1 text-biblio-text hover:bg-biblio-accent px-3 py-2 rounded-md text-sm font-medium transition border border-biblio-text/20">
                                <span>📋</span> Mes Demandes
                            </a>
                        {% endif %}

                        <a href="{{ path('app_cart_index') }}" class="flex items-center gap-2 text-biblio-text hover:bg-biblio-accent px-3 py-2 rounded-md transition font-bold" title="Voir mon panier">
                            <span class="text-xl">🛒</span>
                            <span>Panier</span>
                        </a>

                        {% if app.user %}
                            <div class="relative ml-2">
                                <button type="button" onclick="toggleUserMenu()" class="flex items-center gap-2 bg-biblio-accent text-white rounded-full px-4 py-2 text-sm focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-offset-biblio-dark focus:ring-white transition shadow hover:bg-[#6d4c41]" id="user-menu-button">
                                    <span class="font-bold truncate max-w-[100px]">{{ app.user.nom }}</span>
                                    <svg class="h-4 w-4" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd" /></svg>
                                </button>

                                <div id="user-menu-dropdown" class="hidden origin-top-right absolute right-0 mt-2 w-48 rounded-md shadow-lg py-1 bg-white ring-1 ring-black ring-opacity-5 focus:outline-none z-50">
                                    <div class="px-4 py-2 border-b border-gray-100 bg-gray-50">
                                        <p class="text-xs text-gray-500">Connecté en tant que</p>
                                        <p class="text-sm font-bold text-gray-900 truncate">{{ app.user.email }}</p>
                                    </div>
                                    <a href="{{ path('app_logout') }}" class="block px-4 py-2 text-sm text-red-600 hover:bg-red-50 font-bold">
                                        🚪 Déconnexion
                                    </a>
                                </div>
                            </div>
                        {% else %}
                            <div class="flex gap-2">
                                <a href="{{ path('app_login') }}" class="text-biblio-text hover:bg-biblio-accent px-3 py-2 rounded-md text-sm font-medium transition">
                                    Connexion
                                </a>
                                <a href="{{ path('app_register') }}" class="bg-biblio-light text-biblio-dark hover:bg-white px-3 py-2 rounded-md text-sm font-bold shadow transition border border-biblio-light">
                                    S'inscrire
                                </a>
                            </div>
                        {% endif %}
                    </div>
                </div>
            </div>
        </nav>

        {# --- MESSAGES FLASH --- #}
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 mt-4 w-full">
            {% for label, messages in app.flashes %}
                {% for message in messages %}
                    <div class="rounded-md p-4 mb-4 shadow-sm flex items-start gap-3 animate-pulse
                        {% if label == 'success' %}bg-green-100 text-green-800 border border-green-200{% elseif label == 'error' %}bg-red-100 text-red-800 border border-red-200{% else %}bg-blue-100 text-blue-800 border border-blue-200{% endif %}">
                        <span class="text-xl">
                            {% if label == 'success' %}✅{% elseif label == 'error' %}❌{% else %}ℹ️{% endif %}
                        </span>
                        <span class="font-medium">{{ message }}</span>
                    </div>
                {% endfor %}
            {% endfor %}
        </div>

        {# --- CONTENU PRINCIPAL --- #}
        <main class="flex-grow">
            {% block body %}{% endblock %}
        </main>

        {# --- FOOTER --- #}
        <footer class="bg-biblio-dark text-biblio-text py-6 mt-12">
            <div class="max-w-7xl mx-auto px-4 text-center">
                <p>&copy; {{ "now"|date("Y") }} BiblioHub. Tous droits réservés.</p>
            </div>
        </footer>

        {# --- SCRIPT NAVIGATION --- #}
        <script>
            function toggleUserMenu() {
                const menu = document.getElementById('user-menu-dropdown');
                if(menu) menu.classList.toggle('hidden');
            }
            window.addEventListener('click', function(e) {
                const button = document.getElementById('user-menu-button');
                const menu = document.getElementById('user-menu-dropdown');
                if (button && menu && !button.contains(e.target) && !menu.contains(e.target)) {
                    menu.classList.add('hidden');
                }
            });
        </script>
    </body>
</html>